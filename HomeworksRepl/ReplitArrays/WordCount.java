package HomeworksRepl.ReplitArrays;

import java.util.Arrays;

public class WordCount {
    /*    Create a string "Removes white space from both ends of a string"
        print the count of word in the string  */
    public static void main(String[] args) {
        String remove = "Removes white space from both ends of a string";
      String[] parts = remove.split(" ");
        for (int i = 0; i < parts.length; i++){
        }
        System.out.println(parts.length);
    }
}